@model List<Donation>
@{
    ViewData["Title"] = "Manage Donations";
}

<div class="row mb-4">
    <div class="col-12">
        <h2><i class="fas fa-hand-holding-heart"></i> Manage Donations</h2>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/Admin/Dashboard">Dashboard</a></li>
                <li class="breadcrumb-item active">Donations</li>
            </ol>
        </nav>
    </div>
</div>

<!-- Filter Tabs -->
<ul class="nav nav-tabs mb-3" role="tablist">
    <li class="nav-item">
        <a class="nav-link active" data-bs-toggle="tab" href="#all">All (@Model.Count)</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" data-bs-toggle="tab" href="#pending">Pending (@Model.Count(d => d.Status == "Pending"))</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" data-bs-toggle="tab" href="#approved">Approved (@Model.Count(d => d.Status == "Approved"))</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" data-bs-toggle="tab" href="#completed">Completed (@Model.Count(d => d.Status == "Completed"))</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" data-bs-toggle="tab" href="#rejected">Rejected (@Model.Count(d => d.Status == "Rejected"))</a>
    </li>
</ul>

<div class="tab-content">
    <!-- All Donations -->
    <div id="all" class="tab-pane fade show active">
        @await Html.PartialAsync("_DonationsTable", Model)
    </div>

    <!-- Pending -->
    <div id="pending" class="tab-pane fade">
        @await Html.PartialAsync("_DonationsTable", Model.Where(d => d.Status == "Pending").ToList())
    </div>

    <!-- Approved -->
    <div id="approved" class="tab-pane fade">
        @await Html.PartialAsync("_DonationsTable", Model.Where(d => d.Status == "Approved").ToList())
    </div>

    <!-- Completed -->
    <div id="completed" class="tab-pane fade">
        @await Html.PartialAsync("_DonationsTable", Model.Where(d => d.Status == "Completed").ToList())
    </div>

    <!-- Rejected -->
    <div id="rejected" class="tab-pane fade">
        @await Html.PartialAsync("_DonationsTable", Model.Where(d => d.Status == "Rejected").ToList())
    </div>
</div>

<!-- Reject Modal -->
<div class="modal fade" id="rejectModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Reject Donation</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form id="rejectForm" method="post">
                @Html.AntiForgeryToken()
                <div class="modal-body">
                    <div class="mb-3">
                        <label for="rejectReason" class="form-label">Reason for Rejection</label>
                        <textarea class="form-control" id="rejectReason" name="reason" rows="3" required></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-danger">Reject</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function showRejectModal(donationId) {
            $('#rejectForm').attr('action', '/Admin/RejectDonation/' + donationId);
            $('#rejectModal').modal('show');
        }
    </script>
}
